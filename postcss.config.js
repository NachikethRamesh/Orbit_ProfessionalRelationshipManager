/**
 * PostCSS Configuration
 *
 * Processes CSS through Tailwind CSS (utility class generation)
 * and Autoprefixer (adds vendor prefixes for browser compatibility).
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
